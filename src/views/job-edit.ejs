<!DOCTYPE html>
<html lang="pt-br" translate="yes">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta http-equiv="X-UA-Compatible" content="IE=7" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#41414C" />
    <link rel="shortcut icon" href="/assets/favicon.png" type="image/png" />
    <title>JobsCalc - Editar um Job</title>

    <%- include('components/styles'); %> <%- include('components/fonts'); %>

    <link rel="stylesheet" href="/styles/components/modal.css" />
    <link rel="stylesheet" href="/styles/pages/job.css" />

    <script type="module" defer src="/scripts/job-edit.js"></script>
    <script type="module" defer src="/scripts/validate.js"></script>
  </head>
  <body id="page-job-edit">
    <%- include('components/page-header', {title: 'Editar um job'}); %>

    <div class="container flex animate-up delay-2">
      <main>
        <form id="form-job" action="/job/<%= job.id %>" method="POST">
          <fieldset>
            <legend>Informações do Job</legend>
            <div class="separator light"></div>

            <div class="input-wrapper">
              <label for="name">Nome do Job</label>

              <input
                type="text"
                id="name"
                name="name"
                value="<%= job.name %>"
                required
              />

              <span class="error-wrapper"></span>
            </div>

            <div class="input-group">
              <div class="input-wrapper">
                <label for="daily-hours">
                  Quantas horas <br />
                  por dia vai dedicar ao job?
                </label>

                <input
                  type="number"
                  step="0.1"
                  id="daily-hours"
                  name="dailyHours"
                  value="<%= job.dailyHours %>"
                  required
                />

                <span class="error-wrapper"></span>
              </div>

              <div class="input-wrapper">
                <label for="total-hours">
                  Estimativa de <br />
                  horas para esse Job?
                </label>

                <input
                  type="number"
                  id="total-hours"
                  name="totalHours"
                  value="<%= job.totalHours %>"
                  required
                />

                <span class="error-wrapper"></span>
              </div>
            </div>
          </fieldset>
        </form>
      </main>

      <aside class="card">
        <img src="/assets/money-color.svg" alt="Imagem de Dinheiro" />
        <p>
          O valor do seu projeto ficou em: <br />
          <strong>R$ <%= Math.floor(Number(job.budget)).toFixed(2) %></strong>
        </p>
        <div class="button-group">
          <button
            class="button green"
            form="form-job"
            type="submit"
            title="Salvar Dados"
          >
            Salvar
          </button>
          <a href="#" class="button gray open-modal">
            <img
              src="/assets/trash-24.svg"
              alt="Cancelar cadastro"
              title="Cancelar cadastro"
            />
          </a>
        </div>
      </aside>
    </div>

    <%- include('components/modal', { id: job.id }); %>
  </body>
</html>
